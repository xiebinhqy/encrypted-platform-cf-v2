# ✅ 阶段一：止血清单 — 已完成

> 创建时间：2026-06-14
> 最后更新：2026-06-14
> 完成度：~85%（已部署 staging，待安全审查 + GitHub 发布）

**目标：** 经典版和现代版在 500+ 笔记下稳定运行、数据互通、保存不报错

---

## 已完成项目

### [x] 1. 修复核心报错 + 加固解密链路
- ✅ `decryptCategories` 加固：添加数组类型检查 + `.catch()` 保护
- ✅ `decrypt()` 调用全部添加 `.catch()` 兜底
- ✅ `apiSave`/`createNoteV2`/`updateNoteV2` 添加网络错误兜底

### [x] 2. 优化列表解密性能（解决笔记一多就卡）
- ✅ `_decryptOneNote` 内容字段设为 `""`，不解密 `ciphertext`
- ✅ 笔记内容严格通过 `loadNoteContent()` 按需加载
- ✅ `fetchNotesPaginated` 请求设置 `includeContent: false`

### [x] 3. 修复现代版保存笔记
- ✅ API 层新增 `createNoteV2` 和 `updateNoteV2`
- ✅ `app.js` 的 `saveNote()` 切换为 v2 API
- ✅ 错误处理加固

### [x] 5. 列名统一（v4.0.0）
- ✅ 后端 `note.service.js` 删除自动列名检测
- ✅ 后端 `compat.routes.js` 删除 v1/v2 分支
- ✅ 后端 `database.js` v10：自动建表 + 列名迁移

### [x] 6. 登录体验修复
- ✅ `user.routes.js`：login 401/404 从 `console.error` 降级为 `console.log`
- ✅ `login.js`：401/404 从 `console.error` 降级为 `console.log`

### [x] 7. BUG-012：保存后笔记不显示
- ✅ 后端 `note.routes.js` 的 `handleUpdateNote` 支持 `tags_cipher` 传递
- ✅ 后端 `note.service.js` 的 `updateNote` 接受 `tagsCipher` 参数
- ✅ 前端 PUT 成功后调用 `renderNotes()` 重新渲染 DOM

### [x] 8. BUG-013：一键保存草稿
- ✅ 代码审查确认内联实现已使用正确的 v2 API 路径

### [x] 9. BUG-014：编辑标签时内容丢失
- ✅ `initEditTagDialogEvents` 中保存前通过 `DataLoader.loadNoteContent(note.id)` 按需加载实际内容

### [x] 10. 前端 app.js `_decryptOneNote` 修复
- ✅ 修复了方法损坏 + 重建并行解密 + lazy loading 逻辑

### [x] 11. 后端性能优化（D1 索引 + Worker 配置）
- ✅ `0005_add_performance_indexes.sql` 复合索引验证
- ✅ 慢查询分析 + 复合索引优化（user_id + updated_at）
- ✅ Worker 配置优化
- ✅ API 性能测试脚本

### [x] 12. 前端性能优化（虚拟滚动 + 乐观更新）
- ✅ `frontend/modern/js/perf-optimizer.js` 虚拟滚动模块
- ✅ 乐观更新：保存笔记后立即更新 UI，不等后端响应
- ✅ 局部刷新：只更新受影响的 DOM 元素
- ✅ 骨架屏加载动画

### [x] 13. 数据互通验证 + 版本切换修复
- ✅ `frontend/shared/utils/version-switch.js` 修复经典版 URL 路径
- ✅ `frontend/modern/js/version-switch.js` 修复分离域名为同域路径
- ✅ 验证 sessionStorage 共享（userId + masterKey + authToken）
- ✅ 验证 v1/v2 API 兼容层正常工作

### [x] 14. Staging 环境部署
- ✅ 后端 Worker 部署到 `notes-api-staging`
- ✅ 前端 Worker 部署到 `notes-biji-staging`
- ✅ 12 个后端文件 + 16 个前端文件上传

---

## 新增模块文件（app.js 模块化重构）
```diff
frontend/modern/js/
├── app.js                           # 入口（2660行 → ~200行，2026-06-17 重构）
├── core/
│   ├── state.js                     # 全局状态管理（AppState）
│   └── utils.js                     # 工具函数（Utils + TimeManager + CountAnimation）
├── components/
│   ├── toast.js                     # Toast 提示组件
│   ├── note-editor.js               # 笔记编辑器
│   ├── category-manager.js          # 分类管理
│   └── tag-manager.js              # 标签管理
└── services/
    ├── data-loader.js               # 数据加载
    ├── dashboard-updater.js         # 仪表盘更新
    ├── chart-manager.js             # 图表管理
    ├── sidebar-manager.js           # 侧边栏
    ├── dialog-manager.js            # 弹窗管理 + 个人资料 + 退出登录
    ├── event-manager.js             # 事件绑定
    ├── event-logger.js              # 事件日志系统
    ├── draft-manager.js             # 草稿管理
    ├── loading-overlay.js           # 加载遮罩
    ├── current-editor-state.js      # 编辑器自动暂存
    └── standalone-functions.js      # 独立函数（预览/编辑/切换）
```

## 已修改文件
| 文件 | 修改内容 |
|------|---------|
| `backend/src/routes/user.routes.js` | login 401/404 降级为 console.log |
| `backend/src/services/note.service.js` | updateNote 支持 tags_cipher |
| `backend/src/routes/note.routes.js` | handleUpdateNote 传递 tags_cipher |
| `backend/src/routes/compat.routes.js` | 删除 v1/v2 分支，统一格式 |
| `backend/src/config/database.js` | v10：自动建表 + 列名迁移 |
| `frontend/modern/js/login.js` | 401 降级日志 |
| `frontend/shared/api/note.api.js` | 新增 createNoteV2、updateNoteV2 |
| `frontend/shared/api/index.js` | 导出新 API |
| `frontend/modern/js/perf-optimizer.js` | 虚拟滚动 + 乐观更新 + 局部刷新模块（新增） |
| `frontend/shared/utils/version-switch.js` | 修复版本切换 URL 路径 |
| `frontend/modern/js/version-switch.js` | 修复分离域名为同域路径 |
| `frontend/modern/js/app.js` | **模块化重构：2660行 → ~200行** |
