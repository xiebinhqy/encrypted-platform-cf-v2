# ✅ 阶段一：止血清单 — 已完成

> **创建时间：** 2026-06-14
> **最后更新：** 2026-06-23
> **完成度：** 100%（阶段一已全部完成，转向阶段三本地化部署）

---

**目标：** 经典版和现代版在 500+ 笔记下稳定运行、数据互通、保存不报错

---

## 已完成项目

### [x] 1. 修复核心报错 + 加固解密链路
- ✅ `decryptCategories` 加固：添加数组类型检查 + `.catch()` 保护
- ✅ `decrypt()` 调用全部添加 `.catch()` 兜底
- ✅ `apiSave`/`createNoteV2`/`updateNoteV2` 添加网络错误兜底

### [x] 2. 优化列表解密性能
- ✅ `_decryptOneNote` 内容字段设为 `""`，不解密 `ciphertext`
- ✅ 笔记内容通过 `loadNoteContent()` 按需加载
- ✅ `fetchNotesPaginated` 设置 `includeContent: false`

### [x] 3. 修复现代版保存笔记
- ✅ API 层新增 `createNoteV2` 和 `updateNoteV2`
- ✅ `app.js` 的 `saveNote()` 切换为 v2 API
- ✅ 错误处理加固

### [x] 4~12. 更详细的修复列表
[来源: docs/history/阶段一止血清单-v2.md → 全部 14 项已完成]

### [x] 13. 数据互通验证 + 版本切换修复
✅ 经典版/现代版数据完全互通，sessionStorage 共享

### [x] 14. Staging 环境部署
✅ 后端 Worker 部署到 `notes-api-staging`，前端 Worker 部署到 `notes-biji-staging`

---

## 已修改文件清单（阶段一）

| 文件 | 修改内容 |
|------|---------|
| `backend/src/routes/user.routes.js` | login 401/404 降级为 console.log |
| `backend/src/services/note.service.js` | updateNote 支持 tags_cipher + await 缓存重建 |
| `backend/src/services/category.service.js` | create/update 补充 name_cipher 旧列 |
| `backend/src/routes/note.routes.js` | handleUpdateNote 传递 tags_cipher + 错误日志 |
| `backend/src/routes/v2.routes.js` | compat 路由补充旧列写入 |
| `backend/src/config/database.js` | v14 迁移拆分+阻塞修复 |
| `backend/src/config/constants.js` | CORS 头补充 Authorization |
| `backend/index.js` | 全局错误处理 staging 显示详情 |
| `frontend/modern/js/login.js` | 401 降级日志 |
| `frontend/shared/api/note.api.js` | 新增 createNoteV2、updateNoteV2 |
| `frontend/modern/js/perf-optimizer.js` | 虚拟滚动 + 乐观更新 + 局部刷新模块（新增） |
| `frontend/shared/utils/version-switch.js` | 修复版本切换 URL 路径 |
| `frontend/modern/js/version-switch.js` | 修复分离域名为同域路径 |
| `frontend/modern/js/app.js` | **模块化重构：2660行 → ~200行** |

---

> **文档历史：** v1 [docs/history/阶段一止血清单-v1.md] → v2 [docs/history/阶段一止血清单-v2.md]
> **注意：** 阶段一已全部完成，当前处于阶段三"本地化部署"，请参考 `docs/执行计划.md`